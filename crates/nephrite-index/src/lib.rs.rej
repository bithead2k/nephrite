--- crates/nephrite-index/src/lib.rs
+++ crates/nephrite-index/src/lib.rs
@@ -827,6 +847,7 @@ impl VaultIndex {
             "blocks",
             "inline_fields",
             "footnotes",
+            "attachment_metadata",
         ];
         if !allowed.contains(&table) {
             return Ok(0);
@@ -883,6 +904,9 @@ fn now_ms() -> i64 {
 fn parse_version_for_kind(kind: FK) -> i64 {
     if kind == FK::Markdown {
         MARKDOWN_PARSE_VERSION
+    } else if matches!(kind, FK::Image | FK::Attachment | FK::Other) {
+        // Revisit unchanged attachments when metadata/content extraction grows.
+        1
     } else {
         PROJECT_VERSION.major as i64
     }
